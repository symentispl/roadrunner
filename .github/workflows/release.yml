name: Maven Release

on:
  workflow_dispatch:
    inputs:
      bump:
        description: 'Version bump type'
        required: true
        default: 'patch'
        type: choice
        options:
          - patch
          - minor
          - major

permissions:
  contents: write
  packages: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6.0.2

      # Writes ~/.m2/settings.xml with <server id="github"> for GitHub Packages.
      # server-username/server-password are env var *names* resolved at Maven runtime.
      - name: Set up JDK 25
        uses: actions/setup-java@v5.2.0
        with:
          java-version: '25'
          distribution: 'temurin'
          cache: maven
          server-id: github
          server-username: MAVEN_USERNAME
          server-password: MAVEN_PASSWORD

      - name: Set up mise
        uses: jdx/mise-action@v2

      # Loads SSH_PRIVATE_KEY so maven-release-plugin can push [release] commits
      # and the vX.Y.Z tag via the SSH developerConnection in pom.xml.
      - name: Set up SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Configure git identity
        run: |
          git config user.name  "release-bot"
          git config user.email "release-bot@users.noreply.github.com"

      - name: Run release
        env:
          MAVEN_USERNAME: ${{ github.actor }}
          MAVEN_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
        run: mise run release:${{ github.event.inputs.bump }}

      # maven-release-plugin has just created and pushed the vX.Y.Z tag and
      # left a fresh, already-built checkout of that tag under target/checkout.
      # Run JReleaser from there to publish a GitHub Release and upload the
      # jlink + javaArchive ZIPs assembled during the release build.
      - name: Publish GitHub release with JReleaser
        env:
          JRELEASER_GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        working-directory: target/checkout
        run: ./mvnw -pl roadrunner-app -am -B -DskipTests jreleaser:release
